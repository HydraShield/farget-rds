service: express-rds

plugins:
  - serverless-fargate

custom:
  rds:
    database: demoRDSMysql1
    user: ${env:DB_USER}
    password: ${env:DB_PASSWORD}
  vpc:
    defaultVpcId: vpc-076cdf458d78cbf48
    defaultSecurityGroup: sg-0198d538e73da6a38
    defaultSubnetId: subnet-05117624ef4f1335b
    defaultCidr: 172.31.0.0/20

provider:
  name: aws
  runtime: nodejs18.x
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-2'}
  ecr:
    images:
      rds-express:
        path: ./app-express/
        file: Dockerfile

fargate:
  clusterName: express-fargate-cluster
  containerInsights: true
  memory: '4 GB'
  cpu: 2048
  architecture: X86_64
  vpc:
    assignPublicIp: true
    securityGroupIds:
      - ${self:custom.vpc.defaultSecurityGroup}
    subnetIds:
      - ${self:custom.vpc.defaultSubnetId}
  tasks:
    rds-express-fargate-task:
      name: rds-express-fargate-task
      image: rds-express
      memory: '2 GB'
      cpu: 1024
      vpc:
        securityGroupIds:
          - Fn::GetAtt: [SecurityGroup0, GroupId]
        subnetIds:
          - ${self:custom.vpc.defaultSubnetId}
        assignPublicIp: true
      service:
        desiredCount: 1
        maximumPercent: 200
      cloudFormationResource:
        container:
          PortMappings:
            - ContainerPort: 8080
              HostPort: 8080

resources:
  Resources:
    SecurityGroup0:
      Type: AWS::EC2::SecurityGroup
      Properties:
        GroupDescription: My Security Group for Express App on Fargate
        VpcId: ${self:custom.vpc.defaultVpcId}

    SecurityGroup1:
      Type: AWS::EC2::SecurityGroup
      Properties:
        GroupDescription: My Security Group for RDS
        VpcId: ${self:custom.vpc.defaultVpcId}
        SecurityGroupIngress:
          # - CidrIp: ${self:custom.vpc.defaultCidr}
          - GroupId: !Ref SecurityGroup0
            IpProtocol: tcp
            FromPort: 3306
            ToPort: 3306

    DemoRDSMySQL:
      Type: AWS::RDS::DBInstance
      Properties:
        DBName: ${self:custom.rds.database}
        MasterUsername: ${self:custom.rds.user}
        MasterUserPassword: ${self:custom.rds.password}
        Engine: MySQL
        AllocatedStorage: 5
        DBInstanceClass: db.t2.small
        PubliclyAccessible: false
        VPCSecurityGroups:
          - Fn::GetAtt: [SecurityGroup1, GroupId]
